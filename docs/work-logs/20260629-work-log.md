# 作業ログ 2026-06-29

## 概要

先進医療実績報告（AMCPR1A）の続き。前回保留の housekeeping を main へ反映し、**aCRF HTML 生成をスキルの一部として決定論化**した。git ワークフローを「ツール→main 直接／マッピング成果物→PR #2」に整理。

## 決定事項

- **aCRF 生成をスキル化**（ユーザー確認「都度生成せず品質担保されているか」→ 当時 No）。レイアウトマニフェスト＋決定論レンダラの二層構成を採用し、描画規則をコードに固定。
- **ツール（レンダラ・SKILL.md）はレビュー不要 → main へ直接**。レビュー対象は skill ではなく実際のマッピング結果と過程 → **PR #2 はオープン維持**。
- マニフェスト（`aCRF-layout.json`）を aCRF のソース、HTML を派生物と位置づけ。

## 作成・更新したファイル

- `.claude/skills/draft-sdtm-template/scripts/render_acrf.py`（新規・main `d469b0d`）— aCRF 決定論レンダラ。CSS・HSV ドメイン配色（s=71/255, v=244/255, hue=idx/N）・変数名規則（SPID 接頭辞なし灰／DM 接頭辞なし／他 domain+suffix／Findings の "VAR when --TESTCD=値"）・コントロール・sponsor "*" を固定。`--check` で TemplateInput JSON と変数突合。
- `.claude/skills/draft-sdtm-template/SKILL.md`（更新・main `d469b0d`）— フェーズ5b「aCRF レンダリング」を追加。
- `reviews/AMCPR1A/aCRF-layout.json`（新規・PR #2 `94fb2cb`）— AMCPR1A の aCRF レイアウトマニフェスト（worked example）。
- `reviews/AMCPR1A/aCRF.html`・`output/aCRF-AMCPR1A.html`（再生成）— マニフェストから生成。**手書き時の HO ドメイン色の取り違え（#b0f3f4→正準 #b0f4f4）を是正**。「都度生成のドリフト」の実証例。
- main `d120725`（前回保留分の反映）— `docs/work-logs/20260628-work-log.md`・`ACTION-ITEMS.md`・`.gitignore`（tasks/・attachments/）。

## git 状態

- main: `d120725`（housekeeping）→ `d469b0d`（skill）。
- PR #2（review/amcpr1a-senshin-jisseki）: `94fb2cb`（aCRF マニフェスト化・HO 是正）。**オープン＝レビュー対象**。
- PR #1（Copyright 修正）はマージ済と確認。

## 次回アクション

`ACTION-ITEMS.md` を参照。主待ちはチーム4名のレビュー。
